1. 数据效验
   场景: 用户名或手机号是否注册
   实现: 写个接口,第一个参数是用户名或者手机号,第二个参数是类型,1-用户名,2-手机号,去数据库中差是否存在,存在返回ture,不存在返回false
2. 短信发送验证码
    <1> 在阿里云平台申请签名
        签名就是签发者
    <2> 添加短信模板
    <3> 申请accessKey
        访问阿里云的时候需要使用,类似于我们发情请求时需要公钥是一个道理,拥有公钥才能进行访问