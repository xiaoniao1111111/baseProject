<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.study.mapper.TbUserMapper">

    <select id="queryById" resultType="com.study.domain.TbUser">
        select * from tb_user where id=#{id}
    </select>

    <select id="selectUserPage" resultType="com.study.domain.TbUser">
        select * from tb_user where user_name like  concat('%',#{userName} ,'%')
    </select>

</mapper>
